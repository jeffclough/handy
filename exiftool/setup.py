# If compatibility with legacy builds or versions of tools that don’t support
# certain packaging standards (e.g. PEP 517 or PEP 660) is an issue, this
# simple setup.py script keeps them happy. But keep the configuration in
# pyproject.toml.
from setuptools import setup

setup()
